query GetUserGroupsById($userId: Int, $offset: Int, $limit: Int) {
	group_user(
		where: { userId: { _eq: $userId }, accepted: { _eq: true } }
		offset: $offset
		limit: $limit
		order_by: { group: { auditors_aggregate: { max: { createdAt: desc_nulls_last } } } }
	) {
		...UserGroupFields
	}
}
